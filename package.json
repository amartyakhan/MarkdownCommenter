{
  "name": "markdown-pro-commenter",
  "displayName": "Markdown Pro - Preview, Comment, Edit - All in One place",
  "description": "Add inline comments to Markdown files with a live preview",
  "icon": "icon.png",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/amartyakhan/MarkdownCommenter"
  },
  "publisher": "amartyakhan",
  "version": "0.0.6",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other"],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "markdownCommenter.openPreview",
        "title": "Open Markdown Commenter Preview",
        "icon": "$(comment-discussion)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "markdownCommenter.openPreview",
          "when": "resourceLangId == markdown",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "markdownCommenter.openPreview",
          "when": "resourceLangId == markdown"
        }
      ],
      "editor/context": [
        {
          "command": "markdownCommenter.openPreview",
          "when": "resourceLangId == markdown"
        }
      ]
    },
    "customEditors": [
      {
        "viewType": "markdownCommenter.editor",
        "displayName": "Markdown Commenter",
        "selector": [
          {
            "filenamePattern": "*.md"
          }
        ],
        "priority": "option"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "node esbuild.js --minify",
    "compile": "node esbuild.js",
    "watch": "node esbuild.js --watch"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
